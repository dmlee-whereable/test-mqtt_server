// Response to the Wake Up Command
// Recipient: Control
// Sender: AM
// Communication Protocol: MQTT over 5G Module
syntax = "proto3";
package whereable.t2am;
import "Header.proto";

message WakeUpAck {
	Header header = 1;
}